============
Installation
============

At the command line::

    $ easy_install django-autosequence

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-autosequence
    $ pip install django-autosequence
